import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Elijah Cabello",
  description:
    "Portfolio of Elijah Cabello — Computer Science student and aspiring cybersecurity professional.",
  openGraph: {
    title: "Elijah Cabello",
    description:
      "Computer Science student and aspiring cybersecurity professional.",
    url: "https://elijahcabello.com",
    siteName: "Elijah Cabello",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
